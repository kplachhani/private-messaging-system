export const enum socketMessageEnum {
    authenticated = "socket user authenticated",
    notAuthenticated = "socket user not authenticated",
    userJoined = "user joined ",
    userLeft = "user left",
    userFeeds = "user feeds emitted",
    userFeedsUpdated = "user feeds updated",
    welcome = "welcome to chatServer",
}